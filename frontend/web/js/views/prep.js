/* Prep Plans are now authored with the SAME unified editor as guides/tips
 * (see content.js → SUAR.views.prep, category "prep"). This file is kept only
 * so the existing <script> include doesn't 404; it intentionally does nothing
 * and must NOT reassign SUAR.views.prep. */
window.SUAR = window.SUAR || {};
