/* Notices are now authored with the SAME unified editor as guides/tips/prep
 * (see content.js → SUAR.views.notices, {notice:true}). This file is kept only
 * so the existing <script> include doesn't 404; it intentionally does nothing
 * and must NOT reassign SUAR.views.notices. */
window.SUAR = window.SUAR || {};
